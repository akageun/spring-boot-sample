<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.geun.o.hwp.mybatis.front.temp.dao.TempDAO">

    <select id="selectNow" resultType="java.lang.String">
        SELECT NOW()
    </select>

    <select id="selectProductLog" resultType="kr.geun.o.hwp.mybatis.front.temp.vo.ProductLogVO">
        SELECT * FROM product_log
    </select>

</mapper>

